<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            position: relative;
            overflow: hidden;
        }

        .floating-text {
            position: absolute;
            bottom: -50px;
            font-size: 1.5rem;
            color: rgba(0, 123, 255, 0.7);
            font-weight: bold;
            animation: floatUp 10s infinite ease-in-out;
        }

        .floating-text:nth-child(1) {
            left: 10%;
            animation-duration: 12s;
            animation-delay: 0s;
            transform: rotate(-10deg);
        }

        .floating-text:nth-child(2) {
            left: 30%;
            animation-duration: 8s;
            animation-delay: 2s;
            transform: rotate(15deg);
        }

        .floating-text:nth-child(3) {
            left: 50%;
            animation-duration: 10s;
            animation-delay: 4s;
            transform: rotate(-5deg);
        }

        .floating-text:nth-child(4) {
            left: 70%;
            animation-duration: 14s;
            animation-delay: 6s;
            transform: rotate(20deg);
        }

        .floating-text:nth-child(5) {
            left: 90%;
            animation-duration: 9s;
            animation-delay: 8s;
            transform: rotate(-15deg);
        }

        @keyframes floatUp {
            0% {
                transform: translateY(0) translateX(0) scale(1) rotate(0deg);
                opacity: 1;
            }

            50% {
                transform: translateY(-50vh) translateX(10px) scale(1.2) rotate(5deg);
                opacity: 0.7;
            }

            100% {
                transform: translateY(-100vh) translateX(-10px) scale(1.5) rotate(-5deg);
                opacity: 0;
            }
        }
    </style>
</head>

<body class="bg-light">
    <!-- Floating text -->
    <div class="floating-text">
        <%= student.userName %>
    </div>
    <div class="floating-text">
        <%= student.userName %>
    </div>
    <div class="floating-text">
        <%= student.userName %>
    </div>
    <div class="floating-text">
        <%= student.userName %>
    </div>
    <div class="floating-text">
        <%= student.userName %>
    </div>

    <div class="container py-5">
        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="card-title text-center mb-4">Details of <span class="text-primary">
                        <%= student.userName %>
                    </span></h1>
                <div class="row g-3 align-items-center">
                    <div class="col-md-6 text-center">
                        <img src="/<%= student.photo %>" alt="Student Photo" class="img-fluid rounded shadow-sm"
                            style="max-width: 100%; height: auto; max-height: 300px;">
                    </div>
                    <div class="col-md-6">
                        <div class="p-3 border rounded">
                            <p class="mb-2"><span class="fw-bold">Name:</span>
                                <%= student.userName %>
                            </p>
                            <p class="mb-2"><span class="fw-bold">Email:</span>
                                <%= student.email %>
                            </p>
                            <p class="mb-2"><span class="fw-bold">Gender:</span>
                                <%= student.gender %>
                            </p>
                            <p class="mb-2"><span class="fw-bold">Course:</span>
                                <%= student.course %>
                            </p>
                            <p class="mb-2"><span class="fw-bold">Hobbies:</span>
                                <%= student.hobbies %>
                            </p>
                            <hr class="my-3">
                            <p class="mb-0 text-end">
                                <a href="/download/csv/<%= student._id %>" class="text-primary text-decoration-none">
                                    <i class="bi bi-cloud-download"></i> Download Details in csv
                                </a>
                            </p>
                            <p class="mb-0 text-end">
                                <a href="/download/pdf/<%= student._id %>" class="text-primary text-decoration-none">
                                    <i class="bi bi-cloud-download"></i> Download Details in pdf
                                </a>
                            </p>
                            <p class="mb-0 text-end">
                                <a href="/download/<%= student.photo.split('\\').pop() %>"
                                    class="text-primary text-decoration-none">
                                    <i class="bi bi-cloud-download"></i> Download Student Photo
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="mt-4">
                    <div class="d-grid gap-3">
                        <% if (user.role==='teacher' ) { %>
                            <form action="/delete-student/<%= student._id %>" method="POST">
                                <button type="submit" class="btn btn-danger w-100">Delete Student</button>
                            </form>
                            <form action="/add-topper/<%= student._id %>" method="POST">
                                <button type="submit" class="btn btn-warning">Add to Toppers</button>
                            </form>
                            <a href="/edit-student/<%= student._id %>" class="btn btn-warning w-100">Edit Details</a>
                            <% } %>
                                <a href="/" class="btn btn-secondary w-100">Back to Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
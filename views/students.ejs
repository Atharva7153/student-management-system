<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Students List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .student-list {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .student-item {
            padding: 10px;
            margin: 5px 0;
            border-radius: 5px;
            transition: background-color 0.2s;
        }
        .student-item:hover {
            background-color: #f8f9fa;
        }
        .view-details-btn {
            text-decoration: none;
            padding: 4px 8px;
            border-radius: 4px;
            background: linear-gradient(90deg, #007bff, #ff0000);
            background-size: 200% 200%;
            color: white;
            font-size: 0.9em;
            animation: buttonBG 3s ease infinite;
        }
        @keyframes buttonBG {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        .view-details-btn:hover {
            background-color: #0056b3;
            transform: scale(1.1);
            color: white;
        }
        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: #6c757d;
            text-decoration: none;
        }
        .back-link:hover {
            color: #343a40;
        }
        body {
            background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #fbc2eb, #a18cd1);
            background-size: 400% 400%;
            background-repeat: no-repeat;
            height: 100vh;
            margin: 0;
            animation: gradientBG 15s ease infinite;
        }

        @keyframes gradientBG {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        h1 {
            font-family: 'Arial Black', sans-serif;
            font-size: 2.5rem;
            text-transform: uppercase;
            background: linear-gradient(90deg, #ff6a00, #ee0979, #00c6ff, #0072ff);
            background-size: 300% 300%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: gradientText 5s ease infinite;
            text-align: center;
        }

        @keyframes gradientText {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
    </style>
</head>
<body>
    <div class="container student-list">
        <h1 class="mb-4">List of All Students</h1>
        <div class="list-group">
            <% students.forEach(student => { %>
                <div class="student-item list-group-item d-flex justify-content-between align-items-center">
                    <strong><%= student.userName %></strong>
                    <a href="/studentDetails/<%= student._id %>" class="view-details-btn">View Details</a>
                </div>
            <% }) %>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
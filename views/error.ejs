<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Not Found</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(45deg, #ff9a9e, #fad0c4, #fbc2eb);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .error-container {
            text-align: center;
            color: white;
        }

        .error-container h1 {
            font-size: 6rem;
            font-weight: bold;
            text-shadow: 3px 3px 10px rgba(0, 0, 0, 0.5);
            letter-spacing: 2px;
        }

        .error-container p {
            font-size: 1.8rem;
            font-style: italic;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
        }

        .btn-home {
            margin-top: 20px;
            animation: buttonPulse 2s ease-in-out infinite;
        }

        @keyframes buttonPulse {
            0% { transform: scale(1); box-shadow: 0 0 10px rgba(0, 123, 255, 0.5); }
            50% { transform: scale(1.1); box-shadow: 0 0 20px rgba(0, 123, 255, 0.7); }
            100% { transform: scale(1); box-shadow: 0 0 10px rgba(0, 123, 255, 0.5); }
        }

        .error-container h1, .error-container p {
            animation: textColorChange 5s ease infinite, glowEffect 2s ease-in-out infinite alternate;
        }

        @keyframes textColorChange {
            0% { color: red; }
            50% { color: blue; }
            100% { color: red; }
        }

        @keyframes glowEffect {
            0% { text-shadow: 0 0 10px rgba(255, 255, 255, 0.5); }
            100% { text-shadow: 0 0 20px rgba(255, 255, 255, 1); }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>404</h1>
        <p>Oops! The page you are looking for does not exist.</p>
        <a href="/" class="btn btn-primary btn-home">Go to Homepage</a>
    </div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(270deg, #ff9a9e, #fad0c4, #fbc2eb, #a18cd1);
            background-size: 800% 800%;
            animation: gradientAnimation 10s ease infinite;
        }

        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .card {
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .btn-primary {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        input, select, .form-check-input {
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        input:focus, select:focus, .form-check-input:focus {
            outline: none;
            border: 2px solid transparent;
            animation: borderAnimation 1s linear infinite;
        }

        @keyframes borderAnimation {
            0% {
                border-color: #6c63ff;
                box-shadow: 0 0 5px rgba(108, 99, 255, 0.5);
            }
            25% {
                border-color: #ff9a9e;
                box-shadow: 0 0 5px rgba(255, 154, 158, 0.5);
            }
            50% {
                border-color: #fad0c4;
                box-shadow: 0 0 5px rgba(250, 208, 196, 0.5);
            }
            75% {
                border-color: #fbc2eb;
                box-shadow: 0 0 5px rgba(251, 194, 235, 0.5);
            }
            100% {
                border-color: #6c63ff;
                box-shadow: 0 0 5px rgba(108, 99, 255, 0.5);
            }
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Edit Student Information</h4>
                    </div>
                    <div class="card-body">
                        <form action="/edit-student/<%= student._id %>" method="POST" enctype="multipart/form-data">
                            <div class="mb-3">
                                <label class="form-label">Name:</label>
                                <input type="text" class="form-control" name="userName" value="<%= student.name %>" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Email:</label>
                                <input type="email" class="form-control" name="email" value="<%= student.email %>" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Upload Your Photo:</label>
                                <div class="input-group">
                                    <input type="file" name="photo" accept="image/*" class="form-control" id="photoUpload"
                                     >
                                    <label class="input-group-text" for="photoUpload">Browse</label>
                                </div>

                            <div class="mb-3">
                                <label class="form-label">Gender:</label>
                                <div class="d-flex gap-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="radio" name="gender" value="Male" <%=student.gender==="Male" ? "checked" : "" %>>
                                        <label class="form-check-label">Male</label>
                                    </div>
                                    <div class="form-check">
                                        <input class="form-check-input" type="radio" name="gender" value="Female" <%=student.gender==="Female" ? "checked" : "" %>>
                                        <label class="form-check-label">Female</label>
                                    </div>
                                    <div class="form-check">
                                        <input class="form-check-input" type="radio" name="gender" value="Other" <%=student.gender==="Other" ? "checked" : "" %>>
                                        <label class="form-check-label">Other</label>
                                    </div>
                                </div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Course:</label>
                                <select class="form-select" name="course" required>
                                    <option value="BTech" <%=student.course==="BTech" ? "selected" : "" %>>BTech</option>
                                    <option value="BCA" <%=student.course==="BCA" ? "selected" : "" %>>BCA</option>
                                    <option value="BSc" <%=student.course==="BSc" ? "selected" : "" %>>BSc</option>
                                    <option value="MCA" <%=student.course==="MCA" ? "selected" : "" %>>MCA</option>
                                    <option value="MTech" <%=student.course==="MTech" ? "selected" : "" %>>MTech</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">Select your hobbies:</label>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="hobbies" value="Music" <%=student.hobbies.includes("Music") ? "checked" : "" %>>
                                    <label class="form-check-label">Music</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="hobbies" value="Gaming" <%=student.hobbies.includes("Gaming") ? "checked" : "" %>>
                                    <label class="form-check-label">Gaming</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="hobbies" value="Reading" <%=student.hobbies.includes("Reading") ? "checked" : "" %>>
                                    <label class="form-check-label">Reading</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="hobbies" value="Coding" <%=student.hobbies.includes("Coding") ? "checked" : "" %>>
                                    <label class="form-check-label">Coding</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="hobbies" value="Sports" <%=student.hobbies.includes("Sports") ? "checked" : "" %>>
                                    <label class="form-check-label">Sports</label>
                                </div>
                            </div>

                            <button type="submit" class="btn btn-primary w-100">Update</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>